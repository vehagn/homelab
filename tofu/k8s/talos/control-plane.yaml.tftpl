machine:
  network:
    hostname: ${hostname}
cluster:
  allowSchedulingOnControlPlanes: true
  network:
    cni:
      name: none
  proxy:
    disabled: true
  inlineManifests:
    - name: cilium-install
      contents: |
        ${indent(8, file("bootstrap/cilium-install.yaml"))}
