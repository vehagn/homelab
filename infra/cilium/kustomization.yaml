apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

resources:
#  - namespace.yaml
  - traefik-forward-auth
  - ingress-route.yaml
  - ip-pool.yaml
  - announce.yaml

#patches:
#  # peer-service: "hubble-peer.default.svc.cluster.local:443"
#  - path: patches/hubble-relay-config-peer-service-patch.yaml

helmCharts:
  - includeCRDs: true
    name: cilium
    releaseName: cilium
    repo: https://helm.cilium.io
    valuesFile: values.yaml
    version: 1.14.4
