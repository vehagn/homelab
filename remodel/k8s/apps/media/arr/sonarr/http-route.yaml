apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr-http-route
  namespace: arr
spec:
  parentRefs:
    - name: euclid
      namespace: gateway
  hostnames:
    - "sonarr.euclid.stonegarden.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: sonarr
          port: 80