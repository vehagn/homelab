apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prowlarr-http-route
  namespace: arr
spec:
  parentRefs:
    - name: euclid
      namespace: gateway
  hostnames:
    - "prowlarr.euclid.stonegarden.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: prowlarr-web
          port: 80
