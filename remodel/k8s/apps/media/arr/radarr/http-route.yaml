apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radarr-http-route
  namespace: arr
spec:
  parentRefs:
    - name: euclid
      namespace: gateway
  hostnames:
    - "radarr.euclid.stonegarden.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: radarr-web
          port: 80
