apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-route-euclid
  namespace: argocd
spec:
  parentRefs:
    - name: euclid
      namespace: gateway
  hostnames:
    - "argocd.euclid.stonegarden.dev"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: argocd-server
          port: 80