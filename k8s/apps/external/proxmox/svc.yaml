apiVersion: v1
kind: Service
metadata:
  name: proxmox
  namespace: proxmox
spec:
  ports:
    - name: https
      protocol: TCP
      port: 443
      appProtocol: kubernetes.io/h2c
      targetPort: 8006
