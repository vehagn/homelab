apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: default-ip-pool
spec:
  blocks:
    - cidr: "172.20.10.0/24"
  serviceSelector:
    matchLabels:
      ip-pool: default
